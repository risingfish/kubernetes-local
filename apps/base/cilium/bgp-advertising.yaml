apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: bgp-advertisement-lb
  labels:
    # A label selector used to link this to the peer config
    bgp.cilium.io/advertise: lb-services
spec:
  advertisements:
    - advertisementType: LoadBalancerIP