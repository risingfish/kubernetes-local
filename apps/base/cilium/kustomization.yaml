apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: local-cluster
  namespace: flux-system
resources:
  - namespace.yaml
  - bgp-cluster-config.yaml
  - bgp-peer-config.yaml
  - bgp-advertising.yaml
